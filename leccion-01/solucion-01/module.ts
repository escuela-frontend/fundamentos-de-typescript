// ¡Compilemos este archivo!

// Si tienes todo instalado correctamente, podrás hacerlo con tsc:

// $ ./node_modules/.bin/tsc module.ts
// Veremos un nuevo archivo JavaScript.

var saludar = () => {
  const saludo: string = "Hello";
  console.log(saludo);
};
